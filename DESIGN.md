# 今晚睡哪儿 · 像素系统与动作契约

## 1. 氛围与身份
保留已确认的像素人物、桥下生活和海报配色。蓝衣眼镜轩哥、棕衣红围巾凡哥、绿衣鸭舌帽马哥。pixel/intro-art为复用原语与参考；不重做开场或海报。

## 2. 色彩
DOM沿用style.css：背景#131a20、面板#1f2a32/#26333b、正文#e6dfcc、次级文字#a7b1b3、线条#3e4c53、金#e3bb72、纸#d7cbb1。Canvas沿用pixel.js WHO角色明暗三色、皮肤#d9a577/#b98358/#ecc19a、轮廓#141a1f，道具沿用纸箱棕、工具灰、水蓝、火橙。

## 3. 字体
沿用系统中文字体16px/1.6、按钮至少44px；牌桌Canvas沿用10–12逻辑像素。金额与操作同时有DOM文字，中文标签不裁切。

## 4. 空间与响应式
地图960×540、人物24×38基准、脚底y+37；牌桌480×270、卡片24×34。375/768/1280布局保持，无横向溢出，不覆盖公共牌或按钮。

## 5. 原语与状态
保持px/sprite/portrait/bigPortrait/bridgeScene/screeningScene职责。sprite新增work/beg/smoke/phone/sketch/repair/stall/wash/rest/carry两帧，手臂和道具改变。站立、四帧行走、濒死兼容。地图按真实执行行动呈现，未执行或失败不伪装作业。牌桌发牌、下注、收池、思考仅渲染，不驱动钱、牌或其他规则。

## 6. 动作与交互
专用Canvas机制，无相配React组件、不引入依赖。使用时间戳；行走四帧、路径缓入缓出、到达停止。动作约400–700ms两帧，空闲小动作稀疏；小规模整数像素烟、水花和火星。发牌每张150ms；筹码约350–500ms，与事件文字顺序一致，不泄露暗牌。prefers-reduced-motion: reduce时静态姿势、卡牌和筹码直接落定，操作与结算保持。场景关闭后取消自身rAF/timer；后台页不阻塞推进。

## 7. 可访问性与玩家
桌面键盘、手机触屏、减少动态效果玩家能完成同一流程。动画只是反馈，保留DOM操作、焦点与现有开场跳过/加速。测试使用独立origin避免覆盖用户存档。

## 8. 范围与债务
只补Claude中断的动作和整合验收。纯引擎、数值、存档结构不改。现有固定Canvas与地图1.5倍缩放保留。没有构建步骤；执行node检查、行为测试与真实浏览器帧序列。未测Lighthouse不宣称得分。复用既有v8的1000局模拟报告，动画必须不影响规则。

## 本轮扩展（用户后续指令优先）
- 七个独立街景：营地、老街、回收巷、站前、影院、服务站、河岸。横向走动、路口短过场，同时保留总览与排程。
- 营地为1600逻辑像素宽的可漫游场景；夜账后活动栏保留角色切换、坐下、背包、聊天、纸牌与放映，主动结束夜晚。床铺/物资/火桶/电视对应实际交互。夜间显示使用结算当晚，状态恢复不重复结算。
- 店铺以结构与陈列区分：便利店、早餐摊、回收铺、彩票亭、小酒馆、药房、诊所、浴室、五金店不可只改文字。桶、瓶罐、路人有可点锚点，调用既有规则。
- 现有物品统一像素图标，名称仍用DOM文字；主角与普通路人保留不同形象。河岸钓鱼姿势与竿线对齐水面。
- 用户授权新增钓鱼/自由跨街的规则与数据。马哥熟练度高，其余可练；所有随机仍由确定性引擎处理。
- 新开局命名，局域网共用存档表，跨设备继续；并发存档使用revision冲突保护。夜间metadata随存档保存。
- AI只安排其他队友当前一格，当前操控角色计划保留；AI求助只生成文字与固定选项标签，不裁决奖励。
- 前两章按实际事件给情境引导，不剧透遇到马哥的准确回合。对话提示音跟随现有音量开关，短音、角色不同音色、关闭停音。
- 人物栏默认收缩，显示姓名、关键数值和异常状态；独立展开按钮显示完整状态，展开设置不随选人或小时刷新丢失。
- 生存目标每10天一个阶段，以真实饭源、防雨、口粮、寝具、卫生和应急储备显示准备进度及下一步；章节、熟人委托链和既有结局路线保持不变。当日建议与限时热点为次级提示。
- 街道四向行走，人物独立位置与左右侧身朝向；物品使用和工作显示带专属道具的多阶段动作。城管、混混、店员与行人共享精细像素人体比例，并用服装、发型、配件区分。
- 空闲持续走时，现实60秒为游戏1小时；规划、事件和战斗暂停。每件事配置建议时长，可连续数小时，展示行动精神变化、体力、费用和结束时间。
- 新手引导覆盖长期任务、人物展开、移动、随身物品、连续安排和持续走时；首次冲突单独介绍攻击、防守和撤离。
