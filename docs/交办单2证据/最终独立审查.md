# 交办单2最终独立审查

审查范围：`tests/engine.test.js`、`tests/engine-fixtures.js`、`tests/autoplay.test.js`、`scripts/autoplay.js`、`scripts/autoplay/*.js`，及交办单2引擎、模拟证据。票面测试已在先前独立审查中批准，本文不重复审查。

## 结论

- codeQualityStatus: `WATCH`
- recommendation: `APPROVE`（测试与自动模拟实现）
- blockers: 下述三项为既有引擎契约失败，受本交办单禁止修改引擎的范围约束；它们阻止全套测试全绿，但不是本次测试/模拟实现的回归。

## 成功标准核验

| 标准 | 审查证据 |
| --- | --- |
| TASK1 引擎契约 | 当前 `node --test tests/engine.test.js` 为 26 项：23 通过、3 项既有失败。23 个绿色场景均有敏感性证据：`root-mutation-index.json` 的 18 个临时副本变异均 `redExit=1`、`redAssertion=true`、恢复后 `greenExit=0`；另 5 个 child 变异 RED/GREEN 日志覆盖余下场景。 |
| 玩家可见决策 | `scripts/autoplay/runner.js:168-206` 每次策略调用都传入 `observeState` 投影；`scripts/autoplay/observe.js:41-61` 不暴露票奖、票面或事件内部 roll。用户允许 forecast，且当前投影只含同一玩家界面展示的 `forecast`。`tests/autoplay.test.js:13-34` 覆盖该边界。 |
| 不把失败动作记为成功 | 只有 `settle` 成功后才在 `scripts/autoplay/runner.js:252-254` 调用 `trackPlannedDay` 与 `trackTransition`；彩票、牌局和日现金均从该成功路径归档。购买烟仅在 `buyNow` 成功后计数（`runner.js:171-179`）。 |
| D100 与生存统计 | `runner.js:103-120,264-280` 仅在夜间结算后递增完成日数，`completedDay100` 以实际完成 100 天判定；死亡或未到达检查点的样本不会被误记为完成。`statistics.js:32-41` 对无存活人物的 health/mind/hygiene 过滤后再取分位数。 |
| 600 局正式批次 | `autoplay-runs.json` 的 meta 是每策略 200 局、种子 1000–1199；三个策略各自都为 200 条、首末种子为 1000/1199。`sim-run.json` 记录 `npm run sim` exit 0、65,139.14 ms；脚本自身记录 64,898.34 ms，低于 300 秒。 |
| 报告口径 | 重新导入原始 `autoplay-runs.json` 并执行 `summarizeRuns`，得到的三策略完成数、存活率、现金分位数、死亡原因、断供、愿望、疾病、博彩与 D100 检查点均与 `docs/平衡模拟报告.md` 一致。报告包含每十日中位数、死亡原因、停工、断供、愿望、疾病、日现金分类和建议。 |
| 近期补强 | `docs/交办单2证据/autoplay/compliance-red.log` 显示“先牌后票”与 null 检查点两项均实质失败；`compliance-green.log` 记录恢复后均通过。当前 `node --test tests/autoplay.test.js` 为 8/8 通过。 |

## 实际验证

- 本审查运行 `npm test`：123 tests，120 pass，3 fail，374.689 ms；失败与 `docs/交办单2证据/npm-test-final.log` 相同。
- 本审查运行 `node --check` 于 3 个新增/修改测试与 5 个 autoplay 源文件：全部退出 0。
- `sim-engine-before.json` 与 `sim-engine-after.json` 的 16 个受保护引擎文件 SHA-256 完全一致；模拟没有修改引擎。

## 外部引擎阻塞（不归责本次实现）

1. `tests/engine.test.js:43-45`：现金不足预检返回 `at: null`，未定位 `xuan` 与当前 slot。
2. `tests/engine.test.js:344-350`：公共排程器在 tail 阶段拒绝救援。
3. `tests/engine.test.js:375-381`：死亡后既有 open/reserved 事件 cast 仍保留死者。

这些失败在本次独立 `npm test` 中可重现；交办范围禁止修改 `public/game/**`，故不建议以测试或模拟代码规避。

## 技能视角检查

已应用 `omo:programming` 与 `omo:remove-ai-slops` 的审查标准。测试覆盖真实行为和变异敏感性，未出现删除式、实现镜像、tautological 或提示词文本测试；自动模拟未增加与目标无关的生产数据解析、归一化或未类型化逃生口。`runner.js` 为 271 纯代码行，通用 slop 指引会建议拆分；本项目 `AGENTS.md` 明确约 400 行才需拆分，且本轮要求最小改动，故记录为维护性观察而非违规或验收阻塞。

## Findings

### CRITICAL

无。

### HIGH

无本次测试或模拟实现引入的问题。三个既有引擎失败见“外部引擎阻塞”。

### MEDIUM

无。

### LOW

`scripts/autoplay/runner.js` 接近通用模块规模阈值；后续功能继续增长时，应按“运行驱动”和“统计追踪”职责拆分。

## 限制

本批准仅覆盖交办单2的引擎测试和自动模拟实现，不表示三项既有引擎失败已解决。

## 审查后交付复测补记（主代理）

正式模拟结束后引擎继续并发更新；settle.js:121–126 新增死亡事件清理。本任务未改引擎。主代理重新执行 npm test 得到 123 tests / 121 pass / 2 fail / 0 skipped；原第3项事件 cast 失败已转绿，现金 at 与 tail assign 仍失败。最终日志见 npm-test-final.log。原审查批准范围不变，模拟对应保留的 sim-engine-snapshot.json。
