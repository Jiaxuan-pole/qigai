# 交办单 2 票面测试回执

## 交付

- tests/tickets.test.js：通过真实 generateFace、verifyFace、STYLES 测试 street、bells、goodday。
- 测试在 before 中执行 await loadData()，每种票面覆盖 payout 0、5、10、25、100，每个 payout 各 200 个 ticketId，共 3,000 张票面；每次断言验奖金额、票面总额、非命中结构和同 ticketId 决定性。
- 票种集合由固定 expected styles 驱动，并断言 STYLES 实际导出正好等于 street、bells、goodday；实现误删票种会直接失败。

## 验证

- node --check tests/tickets.test.js：exit 0。
- env -u TICKETS_MODULE node --test tests/tickets.test.js：4 passed、0 failed。输出：docs/交办单2证据/tickets/tickets-final-green.log。
- 全套 npm test 在本任务早期运行时被并行新增的 tests/autoplay.test.js:52 阻断（null !== 5），不是 tests/tickets.test.js；原始输出：docs/交办单2证据/tickets/tickets-full-suite.log。按父代理要求，未在其他代理红绿期间重跑全套。
- LSP diagnostics MCP 因当前 approval policy 为 never 拒绝启动；以 node --check 完成语法检查。

## 测试敏感性：/tmp 定向变异 RED 后清理

所有 probe 均复制到 /tmp/ticket-* 临时目录，从未改动 public；每个 RED 后用原文件覆盖副本。最后删除副本文件并 rmdir，以真实静态 import 再跑 GREEN。

1. street：把非命中格伪装为奖号且奖金置 0，street 非命中位不能等于奖号直接失败。日志：docs/交办单2证据/tickets/tickets-mutation-structural-red.log
2. bells：把非命中行改成三同且奖金置 0，bells 三同判定必须与命中标记一致直接失败。日志：docs/交办单2证据/tickets/tickets-mutation-bells-red.log
3. goodday：把非命中格奖金改为 5，goodday 非命中格不能显示奖金直接失败。日志：docs/交办单2证据/tickets/tickets-mutation-goodday-red.log
4. 总额：将 face.total 改为 ticket.payout + 1，票面总额断言直接失败。日志：docs/交办单2证据/tickets/tickets-mutation-total-red.log
5. 决定性：给每次生成附加递增字段，deepStrictEqual 直接失败。日志：docs/交办单2证据/tickets/tickets-mutation-determinism-red.log
6. 票种集合：删除 goodday 导出，固定三票种 oracle 直接失败。日志：docs/交办单2证据/tickets/tickets-mutation-style-set-red.log

## 引擎问题

未发现票面引擎 bug。
