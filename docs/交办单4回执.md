# 交办单 4 回执

已完成五个纯逻辑模块与对应单元测试，最终只留下指定十个项目新文件。未运行 git、未加依赖，未修改禁改游戏模块、UI、策划数据、脚本或 package.json。Claude 的接线不属于本交办单验收范围。

当前会话未提供命令行 -o 的实际路径，完整回执保存于 /tmp/beggar-task4-receipt.md；最终回复也提供该回执与摘要，未声称已写入未知路径。

## 逐文件说明

| 文件 | 改动及原因 |
| --- | --- |
| public/game/bins.js | 实现种子锁定的 4×4 翻桶棋盘、手套/熟练加成、逐格物品资源现金与脏物结算、弃桶和中文摘要，让翻格顺序不重掷内容。 |
| tests/bins.test.js | 8 项测试覆盖 500 棋盘统计、随机数量边界、手套/熟练、确定性、真实新坏电器与重复结算拒绝。 |
| public/game/salvage.js | 导出四类修复规则，保留 uid/origin；实现可及性、维修成本、卖/留/赠互斥和电视赠予解锁标记。 |
| tests/salvage.test.js | 7 项测试覆盖修复身份保留、电视门槛、回收巷销售、互斥、自用不执行效果、礼物街区与信任增量。 |
| public/game/loans.js | 实现四种真实借物实例、同伴归还、一次延期、每日逾期惩罚与第三日收回，以及 30 天禁借。 |
| tests/loans.test.js | 7 项测试覆盖信任与街区、归还/延期/逾期边界、队友归还，以及空关系对象不产生 NaN。 |
| public/game/camp.js | 实现两功能位的安装/卸载、材料半返、燃料计数、纸板优先消耗与天气取暖阈值。 |
| tests/camp.test.js | 6 项测试覆盖材料、槽位/重复安装拒绝、退款取整、燃料不足、寒冷三单位与缺字段兼容。 |
| public/game/radio.js | 实现有效收音机检测、明日天气和便利店信息、可复现招工、定日预警和八条城市短讯，同日不重复入队。 |
| tests/radio.test.js | 5 项测试覆盖设备容器、广播条数、预警日期、1000 种子招工概率、确定性和末日空预报。 |

## 验证

- 改动前 npm test：153/153，通过；证据 /tmp/beggar-task4-baseline.log。
- 本次五模块专项：33/33，通过；独立审查者另行重跑确认。
- 最终 npm test：187/187，通过，零失败、零跳过；证据 /tmp/beggar-task4-final.log。完整工作区包含 Claude 的并行新增测试，因此总数不只增加本次 33 项。
- 真实 Node 驱动：翻桶取坏收音机→makeItem→修复→互斥自用→营地广播；手机售出现金及账本+24；修理桌→电视修复→赠予许姐 trust 1→4 并解锁；四种借物真实借出→延期→队友归还→重新借出→逾期收回→30天恢复。全部退出 0，证据 /tmp/beggar-task4-driver.log。
- 500 棋盘、8000 格：最大权重偏差 0.7365099009900984 个百分点；电台 1000 种子中 319 条招工。
- 十文件真实 LSP publishDiagnostics：最终无错误、无警告，未使用语法检查冒充 LSP。MCP 工具因 approval=never 拒绝，改由已有 /opt/homebrew/bin/typescript-language-server 通过 stdio 协议诊断。初检仅 bins 有一个未使用内部参数提示，已移除；复查空诊断。证据 /tmp/beggar-task4-lsp.log、/tmp/beggar-task4-lsp-final-delta.log。
- 十文件 node --check、导入限制、无 Date/Math.random/DOM/fetch、无 emoji、每文件小于 400 行：通过；证据 /tmp/beggar-task4-scope.log。
- 独立审查结论 APPROVE / CLEAR，阻塞项为零；本次没有构建命令（项目无构建步骤），没有执行 UI 接线验收。

最终 npm test 输出末尾 10 行：

```text
✔ bells 票面矩阵锁定返还、结构合法且由 ticketId 决定 (18.795042ms)
✔ goodday 票面矩阵锁定返还、结构合法且由 ticketId 决定 (27.075708ms)
ℹ tests 187
ℹ suites 0
ℹ pass 187
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 1168.162458
```

## 行为红测证据（先红后绿）

1. bins 返回契约：revealCell 缺少 gained/done 时断言失败，补齐后 8/8 绿；/tmp/beggar-task4-bins-return-red.log、/tmp/beggar-task4-bins-green.log。
2. salvage 可及性与处置契约：不应限定维修角色，giftTargets 应为 ID 列表，自用不应擅改功能位；真实 ERR_ASSERTION 红后修复，7/7 绿；/tmp/beggar-task4-salvage-contract-red.log、/tmp/beggar-task4-salvage-green.log。最初仅有 TypeError 的 stub 失败未计入严格证据。
3. loans 借用条件、归还、一次延期与逾期回收：初始行为失败，随后转绿；/tmp/beggar-task4-loans-red.log、/tmp/beggar-task4-loans-green.log。
4. camp 安装：canInstall 实际 false、预期 true；/tmp/beggar-task4-camp-red.log，实现后 6/6 绿 /tmp/beggar-task4-camp-green.log。
5. camp 燃料：fuelCount 实际 0、预期 7；同一红/绿日志。
6. camp 取暖：正常足额燃料实际 0、预期 10；同一红/绿日志。
7. radio 实例识别、2–4 条数与 D63–65 预警、30% 招工：初始均行为失败，实现后 5/5 绿；/tmp/beggar-task4-radio-red.log、/tmp/beggar-task4-radio-green.log。
8. loans 空关系回归：按时归还出现 NaN !== 1，随后增加 trust ??= 0；7/7 绿；/tmp/beggar-task4-loans-fallback-red.log、/tmp/beggar-task4-loans-fallback-green.log。

## 接口契约建议（未改公开签名）

1. 翻桶列出的权重合计是 101，不能同时具有正的“其余 empty”。当前保留原始各权重并除以 101，empty 概率 0；如需要空格请明确其权重。已向用户询问，尚无补充数值。
2. disposeOptions(state, uid) 没有 actorId，营地箱无法确定操作角色，当前此查询禁用箱中处置选项。实际 dispose 支持 opts.actorId，在营地可对箱中旧物自用；建议后续给选项查询增加角色上下文。
3. nightWarmthBonus 会自行调用 burnFuel 并扣料；调用方每晚全队只应调用一次，再把结果应用到角色，不能先 burnFuel 再调用它。按“不足给 +4”字面，零燃料也返回 +4；如原意为无燃料 0，应明确修订。
4. bulletin 的 ctx.forecast 使用现有 forecast(seed, day) 返回的未来三天数组，closedTomorrow 支持关闭时段数组或布尔值。调用方先用 hasRadio 判断是否播放；同日重复读取不追加重复线索。
5. 自用 effectKey：phone_remote_chat / camp_radio_broadcast / xuan_headphones_wish / camp_tv_screening；这里只标 kept，不执行聊天、愿望或功能位占用。赠电视给许姐设置 flags.screeningVenueUnlocked=true，并返回 screening_venue_unlocked。
6. tickLoans 在 day 切到新一日后调用；第3个逾期日为每日 -1 加收回额外 -2，30天禁借以收回日为起点，第30天恢复。lastTickDay 防同日重复扣除，延期同时写回实例和记录。
7. 失败返回风格目前按接口分别为 {error} 与借物的 {ok:false,reason}；建议接线层统一适配，后续再单独统一错误契约。

临时运行资源已清理：LSP 进程已 shutdown 并确认退出，无本任务创建的服务/端口/浏览器；工作者额外放入 .omo 的证据副本已移回 /tmp，空目录已移除。保留 /tmp 验证日志及回执供复核。

工作记录：/var/folders/bk/qyw5zwn53pjdp16kys_dtmg80000gp/T/ulw-task4.XXXXXX.md.zyT9Lxb83n。
