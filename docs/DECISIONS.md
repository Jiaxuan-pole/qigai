# 决策记录（Claude 主张 / Codex 意见 / 采纳与理由）

日期 2026-09-16。Codex 顾问线程 01a0a9c4-d658-7db2-b78c-6864a548958c。

## A 技术栈
- Claude：node:http 零依赖静态服务 + /api 模型代理，0.0.0.0:8787；引擎为浏览器 ES module，node --test 共用；localStorage 存档。
- Codex：同意；补四条——静态只开放 public/、代理固定模型与系统提示并限流限时、密钥不依赖另一仓库绝对路径、旧存档导入必须带版本识别；SDK 只移植调用配置并显式关思考。
- 采纳：全部采纳。密钥读取顺序：进程环境变量 > 项目 .env > 兼容回退 kaelis-engine/.env（用户原话指定配置在那里，回退保留但可用 AI_ENV_FILE 覆盖）。

## B 引擎形态
- Claude：单 state、纯函数、rng(seed,key)、按策划 §17 六步结算。
- Codex：回合语义要分清「已完成回合数 / 正在结算的 T / 时段 0..3」；第六步是事务 ID+日志+存档提交由外层做；物品实例补耐用品不消耗、遗物保留身份；疾病算式 +3+2−7−3 不能重复叠加；救援与尾声要显式阶段，随机键绑实体与节点。
- 采纳：全部采纳。state.turn = 已完成回合数，结算中的 T = turn+1；设备类 durable 不消耗；死者背包转为 relic 容器；diseaseStep 与 05 py 一致；phase 增加 'tail'。

## C 内容范围
- Claude：一次做全 100 日。
- Codex：同意最终交付 100 日，但集成验收按 7 日闭环→30 日→100 日；日节点不能只是晨报，每章要有固定节点、两种解法、失败后的继续、缺员替代。
- 采纳：采纳。story.js 里每章至少一个带选择的固定节点。

## D 借鉴 Hobo 的新增
- Claude：翻垃圾桶、全队声望 0-100、三项技能每 6 次升级 +10%。
- Codex：翻垃圾桶有条件同意（桶有固定身份与日存量、代价可预见、资源与回收不同、不洁食物在食用时记暴露）；全队声望不同意，改具体熟人的履约记录；技能倍率不同意（12 次就练满、鼓励刷），成长绑履约与章节解锁新工作。
- 采纳：采纳 Codex 版本。不做全局声望数；relations[npcId] 记信任/帮助/失约，解锁有容量的具体订单；技能改为「熟练解锁」：累计完成某类工作到阈值解锁新岗位/固定位，无倍率。

## E 分工
- Claude：Codex 做 rules.js 移植+测试、AI 校验器、自动对局；Claude 做引擎/UI/内容/服务端/QA。
- Codex：同意；对话与事件两套校验；服务端校验后浏览器还要核场景版本；Claude 明确负责集成测试、存档迁移、数据装载、启动配置；测试补双核销、抢库存、护理去重、连续污秽、夜间救援、尾声、旧 AI 响应；自动对局共用种子、给分位数与停工原因，策略不得偷看未揭示结果。
- 采纳：采纳。rules.js 由 Claude 先写 v1，Codex 线程接管审校+测试（该波内 Claude 不再碰 rules.js）。

## F UI
- Codex 补：热点用真实按钮并有等价列表；常驻显示「给谁/哪格/哪街区」；提交前显示整格预算与用品可达性、预检失败定位格子；手机每时段页同时看三人；排程模板、重复结果折叠、危险时暂停。
- 采纳：全部采纳。

## G 经济
- Codex：不先砍工资；目标普通日扣经常开支后净 +10~30、交付日 +40~80、护理日 −20~60；先补材料供应/恢复/岗位规则再跑基线。
- 采纳：采纳。电量在服务站休整/整理设备/室内剪辑时 +1（上限 5）；木料布料来自回收与翻桶；先做基线模拟再调。

## H 事件导演
- Codex：保留四个默认参数；区分主要事件与提醒/霉运/安静片段；近期模板与类别降权；同模板按关系发展（初识→交付→引荐）；期限从生成后的规划机会算，读档不重置。
- 采纳：采纳。

## I 五个补齐点
1. 全员濒死可自助求助、T400 后真实救援窗口——采纳。
2. 食物按饱食当量算（正餐 30、面包 18、热汤 26），有效份数 = Σ值/30 向下取整——采纳。
3. 电量/木料/布料/零件供应链——采纳（见 G）。
4. 初次感染严重度 12–21、同病种治愈后 3 日不再抽同种、两病伤害相加、每病每回合只护理一次；UI 解释「只护理不休整净 −4」——采纳。
5. 排程模板与简短结算尽早接入；每章选择留下不同结果——采纳。

## 用户中途追加（2026-09-16 当天）
- 人物按排程自己走过去（可观看）：中间面板改为像素城市地图，推进后沿道路行走动画，后台标签页 rAF 停摆时有保底超时落位。
- 操控某人走到店里刮彩票：安排「到店采买」推进后到店弹窗当场买；人已在该街区可直接买（每时段一次附带采买）；票面按《刮刮乐玩法与票种_游戏设计参考》首版三款（街角即开彩/三个铃铛/好日子），购买锁总返还再生成票面，玩家手刮，60% 面积自动露格，核销一次。
- AI 台词去 AI 味：服务端提示词挂载 kaelis-engine 风格模块三张卡（natural-humanized、character-voice、slice-of-life）+ story-deslop 一级禁用句式/词摘录；真机探针 5 秒返回，契约校验通过。
- 流浪汉玩法：翻垃圾桶战利品扩展（瓶罐/烟头/零钱/肥皂头/不洁面包）、捡瓶罐到老周处 1 元/个、举纸板/速写乞讨、服务站免费餐、候车室过夜（可能被赶）、熟人信任解锁订单。

## 平衡（依据 Codex 自动对局报告 docs/平衡模拟报告.md）
- 基线（v2 工资）：均衡策略 100 日现金 p50 6470、精神全程 100、每局染病 10 次 → 工资偏高、精神压力太弱。
- 第一轮：工资 −35%、岗位数减半、工作扣精神 2–3、爱好恢复减半、饥寒伤精神 → 均衡策略全灭（精神崩溃），矫枉过正。
- 第二轮（当前）：工作扣精神 1（复杂/脏活 2）、岗位数回原值、饥寒精神损失封顶 2、夜间恢复 42/22、精神 +3/+1；模拟策略在精神 <35 时晚上做免费爱好。结果见报告更新。
- Codex 引擎测试发现并已修：现金不足预检未定位格（settle.js）、尾声不能经 assign 安排救助（engine.js）、死者残留在热点 cast（settle.js kill）；乞讨现金账本双记（npcs.js）由浏览器 QA 发现并修。
- 与 Codex 的分歧处理：autoplay.test.js 里「保守策略 seed 1000 活过 30 天」与平衡参数耦合，改为只验证恢复机制（≥10 天）；runner 安全回合上限 1000→4000（岗位收紧后预检重试增多）。
- 第三轮（工资 −20%）后三策略仍全灭（均衡首死 p50 D54，死因饥饿/夜间饥寒为主）。分项实验：夜间体力恢复回 50/25 → 无变化（1%）；去掉模拟策略的「精神<35 做爱好」规则 → 均衡 2%、死因转为病情与精神崩溃。结论：固定策略在真实压力下不会自适应，单调参数不能把它救活；已把诊断与参数方案交 Codex 顾问（q2-balance），等答复后定第四轮。当前引擎参数：工资 −20%、工作扣精神 1/2、爱好 +6、夜间体力 50/25、精神 +3/+1、饥寒精神损失封顶 2。

## 平衡第四轮（依据 Codex 顾问答复 q2-balance，2026-09-16）
- Codex 诊断：报告缺分项统计，无法直接归因；最强候选是「愿望长期不处理 + 恢复削弱 → 策略反复做爱好挤占劳动与保暖」；工资单独触发崩盘的可能不能排除；岗位上限证据最弱（jobLimit 为 0）。
- Codex 指出的引擎缺口（已修）：两餐 60 正好等于日耗 60，一次缺餐永远追不回 → 每回合饱食 −10，夜里饱食≤30 且有余粮自动补一餐；无不洁食物也生成肠胃病并写"吃了不干净的东西" → 病种必须匹配暴露（伤口/不洁食物/卫生<60），否则不抽；自动进食不传不洁标签 → eatOne 返回实例；极脏计数按夜累计 → 改按行动回合累计 8 回合；伤口暴露持续保留 → 覆盖后解除；治愈后同种 3 日免疫。
- Codex 参数候选（已采纳）：工资与岗位保留第三轮；工作扣精神 1/2；免费爱好 +8；回营休息体力 25、健康 4、精神 3；服务站休整体力 16；夜间体力 50/25、精神 3/1；身体精神损失封顶 1；夜间饥饿精神惩罚取消。
- Codex 指出固定策略七个缺陷（补库存不补饱食、补货必须一次买齐、采购覆盖帮厨、保守策略缺钱也不多干活、买烟不用/不回应愿望、爱好压过保暖且不备床棚毯、预算不统一）→ 交办单 3 由 Codex 自己修策略并补分项统计（劳动格、收入拆账、精神拆账、阶段分组）。
- 不采纳/暂缓：`rules.hygiene.baseRisk` 0.02→0.005 暂不改 03 JSON（策划真相），先看暴露语义修正后的病例数。

## 平衡第四轮结果（Codex 交办单 3 交付后，Claude 复跑核验）
- 结果（600 局、种子 1000–1199）：保守策略三人存活 100%（D100 现金 p50 90，普通日净 −1）；均衡策略三人存活 100%（现金 p50 3774，普通日净 +47，精神在 40–70 的角色回合占比 84.8%）；赌徒策略 0%（首死 p50 D14，死因饥饿+病情为主）。测试 153 项全绿；模拟退出码 0。
- 判断：存活目标达标；均衡普通日 +47 高于 +10～30 的目标，但保守已是 −1，再砍工资会先伤谨慎玩家，所以本轮不再动工资，后续用支出项（房租押金、设备）做钱的去处。赌徒策略 0% 主要来自它「每人只备一餐」的设定，不是博彩本身；Codex 已指出需要一个只改博彩选择的对照策略，留作下一步。
- 每局染病 13–15 次偏多：两种策略的卫生下限只有 30/42，正好落在皮肤病暴露带（卫生<60）；人类玩家把卫生保持 ≥60 就不抽皮肤病。暂不改 03 JSON 的 baseRisk。
- Codex 报的两个引擎问题已修：营地一条毯子重复给三人加成 → 改为个人寝具按床位顺序一人一条（新增测试）；普通雨夜过夜区开放条件与文案不一致 → 条件补上 rain。
- Codex 本单改动只在 `scripts/autoplay/**`（新增 planning.js、metrics.js）与 `tests/autoplay.test.js`，已核对未碰 `public/**`。回执见 `docs/交办单3回执.md`。

## 机制扩展调研（2026-09-16 晚，用户追问"还有没有好玩的"）
- Claude 调研 Hobo: Tough Life 两份 Steam 指南 + gameplay.tips + GamerSky + TWoM 分析；Codex 补 CHANGE 与 KoL。合并结论与分歧表在 `docs/机制提案.md` 第五节；Codex 全文在 `docs/机制提案_Codex独立调研.md`。
- 采纳 Codex 反对项：不做偷窃坐牢大系统、不加烟瘾等常驻维护条、不做全局声望；保留 Claude 的翻桶揭格与读人乞讨（Codex 未提但证据充分）。
- 待用户从四个包（旧物与手艺 / 街头互动 / 人情与项目 / 营地与信息）中选择后实施。

## 四个机制包落地（2026-09-17）
- 分工：Codex 交办单 4 写五个纯模块 `bins.js`（翻桶棋盘）、`salvage.js`（旧物修复与去处）、`loans.js`（借物契约）、`camp.js`（功能位与篝火）、`radio.js`（收音机播报）+ 33 项测试；Claude 写 `beg.js`（读人乞讨）、`favors.js`（熟人委托链）、拦路小混混事件、卖艺、三人接力宣传片项目、《旧电视归谁》节点、素材片段与剪片，并把全部模块接进结算/引擎/UI。
- 待处理交互（路人对话、翻桶棋盘）在结算后弹出，未处理完不能推进；脚本与自动对局用 `autoResolvePending` 按固定顺序走完（runner 已接）。
- Codex 回执提到：翻桶权重表原合计 101，按比例归一化后 `empty` 概率为 0；接受。
- 浏览器实测：读人乞讨（3 人 2 中、18 元）、两只桶各翻 6 格、营地修理桌安装均无报错；187 项测试全绿。

## 三条已知边界收口（2026-09-17，顾问答复 a4-boundaries）
- A 染病频率。Claude 主张：改 03 JSON 风险表并在引擎加「连续 ≥2 晚卫生<60 才抽皮肤病」。Codex 意见：先只调风险表，`baseRisk` 保持 0.02，档位改 `[[60,0],[40,0.005],[20,0.02],[0,0.16]]`（粗估 6.2 例/局）；连续脏夜规则不能保证达标、且要另定「白天洗到 60 是否清零」，作为独立规则以后再议；改 JSON 必须同步策划正文 §9.2 与 `tests/rules.test.js` 的具体数值，「05 校验能过」不算验收。采纳 Codex：只改风险表，三处同步（01 正文、03 JSON、rules.test）；40 局探针（种子 1000–1039）保守 13.53→6.30 例/局、均衡 14.68→6.85，全部是皮肤病，发病时卫生带 40–59 与 20–39 各半；05 校验 32 项 PASS。
- B 自动对局用新机制。Claude 主张：新增 `scripts/autoplay/mechanics.js`，翻桶/读人乞讨/摆摊/旧物/count 委托，按 hint 反推路人类型。Codex 意见：hint 反推是熟练玩家策略、不算作弊；必须扩展 observe 视图与指标，pending 分支要逐步走 `begStep`/`binReveal` 而不是 `autoResolvePending`；翻桶停止规则每格前检查、计数分行动/桶/揭格、资源要闭环（卖瓶罐、吃脏面包、用肥皂）；摆摊别只替换 slot1 的 scavenge；修理桌只在有电视时建；卖旧物要在回收巷；count 委托由结算自动发奖；报告要触发样本数/净收益/机会成本；赌徒 0% 无约束力，补「只改博彩选择」的对照策略。采纳 Codex 全部意见，写进交办单 5（`docs/交办单5回执.md`），Codex 执行。
- C 电视放映场面。Claude 主张：短片有身份（来源三段→片名），营地夜里烧一单位燃料放片，每部首映一次、隔五天重播，首映凡哥 +8 其他 +4。Codex 意见：数值可以（重播改凡哥 +3、其他 +2）；关键约束是全队两次放映至少隔 5 天且营地与许姐场地共用，否则多部轮播天天回血；首映才回应 `fan_seen` 与给客人信任；电视按留用实例找（`keep` 不会自动搬进营地箱）；许姐场地事件要与营地共用身份/间隔/首映记录，不能保留独立的 +10 入口；篝火先烧，剩一单位才放映。采纳 Codex 全部修正：新增 `public/game/screening.js`（`recordFilm`/`filmName`/`canScreen`/`pickFilm`/`screenFilm`/`nightScreening`），`flags.lastScreeningDay` 全队共享，`small_screening` 事件有短片时走同一条放映逻辑、没短片走原效果但同样占用间隔，`xuVenue`/`screeningVenueUnlocked` 让该事件权重 ×2（此前旗子无人读）。浏览器实测：第 7 天营地首映《旧影院的人》，观众三人加老陈，凡哥 45→58（含床位与休息），燃料 8→5（篝火 2 + 放映 1），夜话模型台词接上了「片子放完」的事实。
- D 顺序。Claude 主张 A→B 串行、C 并行。Codex 意见：验收串行（原版→A→A+B→A+B+C 同种子），开发可隔离副本并行，sim 入口会覆写报告要先存基线。采纳：A 先落地并用探针复测；B 起线程时以含 A 的工作树为基线、先复制报告为 `_v6基线`；C 对自动对局无影响（策略从不修电视、不选放映事件），所以 C 与 B 并行不污染测量。

## 标题页与开场动画（2026-09-17，用户指令）
- 用户提供海报《今晚睡哪儿》（桥下三人围着油桶火）作标题页背景：Claude 直接落地，`public/title.css` 覆盖标题页样式（不动 `style.css`，因为 Codex 交办单 6 正在改其中的 intro 规则），桌面端海报铺满、菜单在底部渐暗栏；用户随后再给一张竖版海报给手机：整张放顶部（不裁标题字，1122×1402 按宽铺）、底边 28px 渐变接深色区、菜单两列。浏览器截图桌面 1440×860、手机 390×844 与 375×667 确认（小屏免责声明落到首屏之下，可滚动）。
- 开场过场动画用户嫌效果不好、要求加 SVG 动画，并指定交给 Codex（交办单 6）：内联 SVG 重做三层视差天际线、天色渐变、雾、镜头推拉、人物姿势过渡与呼吸、打火机三下、烟、车灯落灰、马克笔按文案节奏画纸箱、走路步态、标题逐字浮现；文案与拍序不改；Claude 负责浏览器验收。

## 开场重做与五路并行（2026-09-17 晚）
- 用户看了 Codex 交办单 6 的 SVG 开场：「人物太难看，还不如像素的」，定为「还是原来像素的，一点一点打磨精细，SVG 可以用一点点」，并让 Claude 直接做。处理：SVG 版整体归档到 `docs/archive/intro-svg-codex/`（intro.js/intro-scene.js/测试/样式），像素版原样恢复后新写 `public/ui/intro-art.js`（与地图共用的 pixel.js 分开，避免拖累地图），第一轮打磨：天色分带渐变与地平线暖光、两层视差城市、抖动雾带、黑屏拖行李箱剪影、桥面接缝落灰、带涂鸦与横幅的桥柱、水洼、橘猫、24×46 整数像素人物（蹲/坐/弯腰/四帧走路、手机冷光/打火机/烟/马克笔）、时间制镜头缓动；SVG 仅暗角与薄雾叠层。浏览器逐拍截图核对后又修了三处：镜头过近露黑边（改 1.18/1.3 并把地面画到 300）、蹲姿像站着（大腿横放膝盖在前）、白天低空太黄（楼缝里像金条）。
- Codex 线程用 `-o` 指到回执文件的教训：线程结束时最终消息会覆写该文件，交办单 5/6 的详细回执因此丢失，现存版本是从线程消息恢复的；以后 `-o` 单独指向 scratchpad，回执路径另给。
- 用户指令「开 fable 子代理并行」：五个分叉各管一块文件范围（牌局 cards/cardtable；闹事 trouble/events/radio；美术 pixel/render/map/modals/flow；音效 audio + app 单行 + index 一个按钮；引导 tutorial + app 单行），app.js 只允许单行插入、style.css 只许末尾追加，README/DECISIONS 由主线合并。已交付并核过截图：引导（两组 11 步，桌面与手机）、音效（19 个 CC0/CC-BY 文件 + 合成兜底，`window.jwsnAudio`）、美术（人物重画、8 种表情、大头像、地图与桥下细化，`tests/pixel.test.js` 用假 2D 上下文跑遍姿势与表情）。
- 主线补的小活：玩法说明里加「重看新手引导」；开场接可选声音（轮子磕地、街道底噪）。
- 牌局分叉交付：`public/game/cards.js`（牌型/流程/边池/马哥牌运 `MA_LUCK.chance=0.4`）、`cards-ai.js`（三种性格，只看自己牌与桌面）、`public/ui/cardtable.js`；引擎追加 `canCardNight/startCardNight/cardAction/nextHand/finishCardNight`，`pending.cards` 与 `autoResolvePending` 收桌；13 项测试；调研文档 `docs/牌局调研.md`（规则来源：Teen Patti/Texas hold'em/Poker probability 维基、百度百科与知乎的炸金花流程）。用户定「马哥牌运一直存在」→ 不论谁操作都生效。主线真机联调：标题→跳过开场→引导→注入第 7 天营地夜→夜账→「今晚打牌吗」→操作马哥打一手火柴棍炸金花（轩哥弃、凡哥比牌赢 10）→收牌结算（精神 +6、体力 −5、pending 清空）→夜话，无报错。
- 闹事分叉交付：`public/game/trouble.js` 常量表在顶部 `C`；`street_thugs` 重写、新增 `chengguan_sweep`、营地清场晨间节点、收音机清街预告；9 项测试；1000 局模拟退出 0，四个策略存活率仍 100%。已知缺口：城管硬顶输了的「当日禁摊」借用摆摊冷却不分街区；「收拾走人」做成没收瓶罐纸板；自动对局不选这些事件。
- 主线复跑：`npm test` 257 项全绿；`npm run sim`（v8）保守/均衡/博彩对照/均衡新手三人存活 100%、旧赌徒 0%，均衡终局现金 p50 5235、每局疾病 8.4（翻桶带来的肠胃病计入）。
